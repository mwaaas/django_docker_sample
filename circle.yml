machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t mwaaas/testing:$CIRCLE_SHA1 .

test:
  override:
    - docker run mwaaas/testing:$CIRCLE_SHA1 python manage.py test

deployment:
  elasticbeanstalk:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - ./deploy.sh $CIRCLE_SHA1